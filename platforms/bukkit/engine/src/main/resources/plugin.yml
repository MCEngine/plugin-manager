name: ${name}
version: ${version}
main: ${main}
api-version: '${apiVersion}'
folia-supported: true
author: ${author}
description: Plugin manager for SpigotMC, PaperMC and Folia
commands:
  ${command}:
    description: The example command this plugin ships with
    usage: /${command} <ping|greet [text]>
    aliases: [${commandAlias}]
